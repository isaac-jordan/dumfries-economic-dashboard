<div class="padding" ng-controller="categoryController">
    <div class="full col-sm-9">
        <div class="row">
            <div class="col-sm-12">
                <div class="panel-group">
                    {% for graph in graphs %}
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            {{ graph }}
                        </div>
                    </div>
                    {% empty %}
                    <p>You have no visualisations.</p>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>